CREATE DATABASE IF NOT EXISTS `example-app`;
USE `example-app`;

            
        CREATE TABLE IF NOT EXISTS `test` (
            test_id INT(11) NOT NULL PRIMARY KEY AUTO_INCREMENT,
            test_field VARCHAR(100) 
        );